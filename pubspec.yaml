name: flutter_application_depi
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  dartz: ^0.10.1
  http: ^1.3.0
  lottie: ^3.2.0
  firebase_core: ^3.12.1
  animated_splash_screen: ^1.3.0
  firebase_messaging: ^15.2.4
  firebase_auth: ^5.5.1
  cloud_firestore: ^5.6.5
  google_sign_in: ^6.2.2
  awesome_dialog: ^3.2.1
  wheel_chooser: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
   - assets/
   - assets/images/
